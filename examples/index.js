const addReaction = require('./addReaction.json');
const removeReaction = require('./removeReaction.json');
const challenge = require('./challenge.json');

module.exports = {
  removeReaction,
  addReaction,
  challenge
}